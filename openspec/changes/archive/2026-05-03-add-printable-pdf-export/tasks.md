## 1. Print route + minimal layout

- [x] 1.1 Create `app/characters/[id]/print/layout.tsx` returning a minimal layout: HTML / body shell with the Cinzel + Spectral fonts and `app/globals.css` only — no header, no footer, no toaster
- [x] 1.2 Create `app/characters/[id]/print/page.tsx` that unwraps `params: Promise<{id: string}>` via `use()`, loads the character via `LocalCharacterStore.load(id)`, and renders `<PrintableSheet character={c} />` (or a "character not found" + Home link if the load returns null)
- [x] 1.3 Add a small no-print control bar above the sheet: "Print again" button (re-fires `window.print()`) and a `← Back to sheet` link to `/characters/[id]`. Both use `class="no-print"`
- [x] 1.4 In the same page, fire `window.print()` exactly once on mount via `useEffect` with a `setTimeout(..., 250)` so fonts settle. Guard with a ref so React strict-mode double-mount doesn't re-fire it

## 2. PrintableSheet component

- [x] 2.1 Create `components/sheet/printable-sheet.tsx` exporting `PrintableSheet({ character })`
- [x] 2.2 Render an Identity block: Cinzel name (large), then `Origin (subchoice) · Background · Level X Class (Approach)` byline, with one `<OrnateDivider>` underneath
- [x] 2.3 Render the Abilities block: 6 ability columns with score + modifier (use `computeFinalAbilities`)
- [x] 2.4 Render the Saving Throws block: 6 rows (one per ability) with proficiency marker (filled vs outlined dot) and modifier (use `computeSavingThrows`)
- [x] 2.5 Render the Combat block: max HP, hit die size, prof bonus, initiative, speed, corruption threshold (one tight row of plain pills)
- [x] 2.6 Render the Skills block: all 18 skills in a 3-column grid with proficiency marker, ability shorthand, and modifier (use `computeSkillScores`)
- [x] 2.7 Render the Features block: origin features (+ subchoice) → background feature → class L1 features → approach L1 features → per-level class + approach features (use `computeFeatures` if it covers everything; otherwise read the same sources the regular sheet uses)
- [x] 2.8 Render Boons + Burdens blocks (skip the section if the array is empty); each card shows name + full description, with the `+1 ABILITY` suffix on choice-boons and fixed-ability boons, just like the regular sheet
- [x] 2.9 Render the Feats block (skip if empty): resolve via `BOON_BY_ID` for ids that match boons; show `change-self` and `fighting-style:*` ids in a "Special" subgroup with the same logic the regular `<FeatList>` uses
- [x] 2.10 Render the Spells block (only if `c.spellPicks` has any entries): tradition label, then a compact per-level list of spell names (no descriptions), grouped under "Cantrips / 1st / 2nd / …" subheadings
- [x] 2.11 Render the Equipment block: class equipment picks (with the chosen alternative resolved) + background equipment summary
- [x] 2.12 Render the Identity tropes block: personality, ideal, bond, flaw
- [x] 2.13 Render a Notes block with a "Notes" header and the character's `c.notes` text (render the header + empty content area when `c.notes` is blank — leaves room for handwritten additions)
- [x] 2.14 Render a footer line: `Generated by symbarator v<APP_VERSION> on <YYYY-MM-DD>` in faint grey; pull `APP_VERSION` from `lib/version.ts` and the date from `new Date().toISOString().slice(0, 10)`
- [x] 2.15 Apply `break-inside: avoid` (Tailwind `break-inside-avoid`) to every section wrapper so the printer doesn't split a section mid-paragraph
- [x] 2.16 Use only paper-friendly classes throughout: `bg-white`, `text-[#1d1814]`, no gradients, no parchment fills, no shadows. Section headers use a thin `border-b border-[#7a1f1f]` underline as the only crimson element

## 3. Print stylesheet + page setup

- [x] 3.1 Create `app/characters/[id]/print/print.css` and import it from the print layout. Define `@page { size: A4; margin: 12mm 14mm; }` and a `@media print { body { background: white; color: #1d1814; } .no-print { display: none !important; } }` block
- [x] 3.2 Verify Tailwind's print preflight isn't fighting our overrides; if it is, scope an `@layer base` reset in `print.css`
- [x] 3.3 Eyeball the output via Chrome's print dialog → "Save as PDF" on a Mystic L5 fixture; confirm the page is 1-2 sides, white background, text legible, no parchment bleed, no truncated sections

## 4. Sheet entry point

- [x] 4.1 Add a "Print" link to the action bar in `app/characters/[id]/page.tsx`, sitting next to "Export JSON" and "Level Up". Use `<a href="/characters/<id>/print" target="_blank" rel="noreferrer">` so popup blockers don't catch it
- [x] 4.2 Style the link consistently with the existing `Button variant="outline" size="sm"` pattern used by Export JSON

## 5. E2E coverage

- [x] 5.1 Create `e2e/print.spec.ts` and seed a `mysticAtL1` fixture
- [x] 5.2 Test: navigating to `/characters/<id>/print` renders the character's name and key sections (Abilities, Skills, Spells)
- [x] 5.3 Test: live state (currentHp readout, slot pips, death saves panel) is NOT rendered on the print route
- [x] 5.4 Test: clicking "Print" on the regular sheet opens the print route in a new tab (assert `target="_blank"` on the anchor; Playwright doesn't observe the actual new tab cleanly, so anchor inspection + URL navigation is the workable assertion)
- [x] 5.5 Test: a character with no boons/burdens/feats hides those sections (no empty headers) but always shows the Notes block
- [x] 5.6 Confirm the existing 35-test suite still passes

## 6. Verification

- [x] 6.1 Run `npx tsc --noEmit` and confirm no new errors
- [x] 6.2 Run `npm run build` and confirm the print route is included in the route listing
- [x] 6.3 Run `npm run test:e2e` — full suite green
- [x] 6.4 Run `openspec validate add-printable-pdf-export --strict` and confirm clean
